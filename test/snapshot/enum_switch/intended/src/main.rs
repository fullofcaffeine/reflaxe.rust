// Generated by reflaxe.rust (POC)

use std::cell::RefCell;
use std::rc::Rc;

type HxRef<T> = Rc<RefCell<T>>;

mod action;
mod sys;
use crate::action::Action;
use crate::sys::Sys;

fn main() {
    let mut a: Action = Action::Move(2);
    let mut s: String = match a.clone() {
        Action::Move(_) => {
            let mut _g: i32 = match a.clone() {
                Action::Move(__p) => __p,
                _ => unreachable!(),
            };
            {
                let mut d: i32 = _g;
                format!("{}{}", String::from("move:"), d)
            }
        }
        Action::Toggle => String::from("toggle"),
        _ => todo!(),
    };
    println!("{}", s);
}
