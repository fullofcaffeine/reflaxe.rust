class Main {
	static function main():Void {
		var down:Int = Math.floor(Std.parseFloat("3.9"));
		var negative:Int = Math.floor(Std.parseFloat("-1.2"));
		Sys.println(down);
		Sys.println(negative);
		Sys.println(down + negative);
	}
}
