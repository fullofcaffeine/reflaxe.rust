// Generated by reflaxe.rust (POC)

pub const __HX_TYPE_ID: u32 = 0xa8670f10u32;

#[derive(Debug)]

pub struct PathBufTools {}

impl PathBufTools {
    pub fn from_string(s: String) -> std::path::PathBuf {
        return std::path::PathBuf::from(s);
    }

    pub fn join(p: &std::path::PathBuf, child: String) -> std::path::PathBuf {
        return (p).join(child.as_str());
    }

    pub fn push(p: &std::path::PathBuf, child: String) -> std::path::PathBuf {
        return {
            let mut __p = (p).clone();
            __p.push(child.as_str());
            __p
        };
    }

    pub fn to_string_lossy(p: &std::path::PathBuf) -> String {
        return (p).as_path().to_string_lossy().to_string();
    }

    pub fn file_name(p: &std::path::PathBuf) -> Option<String> {
        return (p).file_name().map(|s| s.to_string_lossy().to_string());
    }
}
