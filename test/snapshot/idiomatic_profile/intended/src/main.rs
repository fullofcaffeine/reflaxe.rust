// Generated by reflaxe.rust (POC)

use std::cell::RefCell;
use std::rc::Rc;

type HxRef<T> = Rc<RefCell<T>>;

mod sys;
use crate::sys::Sys;

fn main() {
    let a: i32 = 1;
    let b: i32 = 2;
    let c: i32 = (a + b);
    println!("{}", c);
    let mut i: i32 = 0;
    while (i < 2) {
        i = (i + 1);
    }
}
