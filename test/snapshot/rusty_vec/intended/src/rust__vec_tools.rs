// Generated by reflaxe.rust (POC)

use crate::HxRef;
use std::cell::RefCell;
use std::rc::Rc;

pub const __HX_TYPE_ID: u32 = 0xb874255eu32;

#[derive(Debug)]

pub struct VecTools {}

impl VecTools {
    pub fn fromArray<T: Clone>(a: Vec<T>) -> Vec<T> {
        return a.clone();
    }

    pub fn toArray<T: Clone>(v: Vec<T>) -> Vec<T> {
        return v.clone();
    }

    pub fn len<T>(v: Vec<T>) -> i32 {
        return v.len() as i32;
    }

    pub fn get<T: Clone>(v: Vec<T>, index: i32) -> Option<T> {
        return v.get(index as usize).cloned();
    }

    pub fn set<T>(v: Vec<T>, index: i32, value: T) -> Vec<T> {
        return {
            let mut __v = v;
            __v[index as usize] = value;
            __v
        };
    }
}
