[package]
name = "hx_app"
version = "0.0.1"
edition = "2021"

[dependencies]
hxrt = { path = "./hxrt", default-features = false, features = ["async", "async_tokio", "core", "io"] }
tokio = { version = "1", features = ["rt", "time"] }
