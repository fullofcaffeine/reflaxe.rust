// Generated by reflaxe.rust (POC)

use std::cell::RefCell;
use std::rc::Rc;

type HxRef<T> = Rc<RefCell<T>>;

mod sys;
use crate::sys::Sys;

fn main() {
    let mut i: i32 = 0;
    let mut sum: i32 = 0;
    while (i < 5) {
        sum = (sum + i);
        i = (i + 1);
    }
    println!("{}", sum);
}
