[package]
name = "hxrt"
version = "0.0.1"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", default-features = true }
mio = { version = "0.8", features = ["os-poll", "net"] }
socket2 = "0.5"
hostname = "0.4"
dns-lookup = "2"
parking_lot = "0.12"
