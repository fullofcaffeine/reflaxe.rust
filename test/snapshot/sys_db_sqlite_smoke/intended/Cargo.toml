[package]
name = "hx_app"
version = "0.0.1"
edition = "2021"

[dependencies]
hxrt = { path = "./hxrt", default-features = false, features = ["core", "db", "io"] }
rusqlite = { version = "0.38", features = ["bundled"] }
