// Generated by reflaxe.rust (POC)

use crate::HxRef;
use std::cell::RefCell;
use std::rc::Rc;

pub const __HX_TYPE_ID: u32 = 0xce0a9f5fu32;

#[derive(Debug)]

pub struct FileSystem {}

impl FileSystem {
    pub fn exists(path: String) -> bool {
        return std::path::Path::new(path.as_str()).exists();
    }

    pub fn isDirectory(path: String) -> bool {
        return std::path::Path::new(path.as_str()).is_dir();
    }

    pub fn createDirectory(path: String) {
        {
            std::fs::create_dir_all(path.as_str()).unwrap();
        };
    }

    pub fn deleteFile(path: String) {
        {
            std::fs::remove_file(path.as_str()).unwrap();
        };
    }

    pub fn deleteDirectory(path: String) {
        {
            std::fs::remove_dir(path.as_str()).unwrap();
        };
    }

    pub fn readDirectory(path: String) -> Vec<String> {
        return {
            let mut out: Vec<String> = Vec::new();
            for entry in std::fs::read_dir(path.as_str()).unwrap() {
                let name = entry.unwrap().file_name().to_string_lossy().to_string();
                out.push(name);
            }
            out
        };
    }
}
