class Main {
	static inline function add(a:Int, b:Int):Int {
		return a + b;
	}

	static function main() {
		var total = add(20, 22);
		if (total == -1) {
			var impossible = total;
			if (impossible == 0) {}
		}
	}
}
