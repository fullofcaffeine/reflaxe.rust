# Vendored Reflaxe framework (local)
#
# Provides `-lib reflaxe` for this repo's lix scope without requiring haxelib.
# Paths are resolved relative to the current working directory (CWD).
# Use `${SCOPE_DIR}` so it works from any subdirectory.

-cp ${SCOPE_DIR}/vendor/reflaxe/src/

-D reflaxe=4.0.0-beta
